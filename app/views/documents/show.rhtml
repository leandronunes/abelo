<% for column in Document.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @document.send(column.name) %>
</p>
<% end %>

<%= button('back', _('Back'), :list, :action => 'list') %> 
<%= button('edit', _('Edit'), :edit, :action => 'edit', :id => @document) %> 
