<h2>Editing mass mail</h2>

<%= start_form_tag :action => 'update', :id => @mass_mail %>
  <%= render :partial => 'form' %>
  <div class='button_bar'>
    <%= button _('Save'), :save %>
    <%= button _('Cancel'), :cancel, :action => 'list' %>
  </div>
<%= end_form_tag %>
