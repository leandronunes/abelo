<%= title _("%s in %s") % [@entry.type, @entry.date] %>

<%= display_show_info @entry, :class => 'view_data', :product_id => @product.id %>

<%= display_show_info_options @entry, :id => @product.id %> 

