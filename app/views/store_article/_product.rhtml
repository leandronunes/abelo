<%= category_hierarchy @product.category %> 

<%= subtitle @product.description %>
<% @product.images.each do |i|%>
<%= show_image i %>
<% end %>
<ul>
  <li> <%= product_title(t(:price:)) %>  <%= @product.sell_price  %></li>
  <li> <%= product_title(t(:unit:)) %> <%= @product.unit_measure.name %></li>
  <% if !@product.size.blank? %>
    <li>  <%= product_title(t(:size:)) + @product.size %></li>
  <% end %>
  <% if !@product.color.blank? %>
    <li> <%= product_title(t(:color:)) + @product.color %></li>
  <% end %>
</ul>
